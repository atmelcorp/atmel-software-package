# CFLAGS for configuration

ifeq ($(CONFIG_SOC_SAMA5),y)
CFLAGS += -DCONFIG_SOC_SAMA5
IAR_FLAGS += CONFIG_SOC_SAMA5
endif
ifeq ($(CONFIG_SOC_SAMA5D2),y)
CFLAGS += -DCONFIG_SOC_SAMA5D2
IAR_FLAGS += CONFIG_SOC_SAMA5D2
endif
ifeq ($(CONFIG_SOC_SAMA5D21),y)
CFLAGS += -DCONFIG_SOC_SAMA5D21
IAR_FLAGS += CONFIG_SOC_SAMA5D21
endif
ifeq ($(CONFIG_SOC_SAMA5D4),y)
CFLAGS += -DCONFIG_SOC_SAMA5D4
IAR_FLAGS += CONFIG_SOC_SAMA5D4
endif
ifeq ($(CONFIG_SOC_SAMA5D27),y)
CFLAGS += -DCONFIG_SOC_SAMA5D27
IAR_FLAGS += CONFIG_SOC_SAMA5D27
endif
ifeq ($(CONFIG_SOC_SAMA5D44),y)
CFLAGS += -DCONFIG_SOC_SAMA5D44
IAR_FLAGS += CONFIG_SOC_SAMA5D44
endif

ifeq ($(CONFIG_BOARD_SAMA5D2_VB),y)
CFLAGS += -DCONFIG_BOARD_SAMA5D2_VB
IAR_FLAGS += CONFIG_BOARD_SAMA5D2_VB
endif
ifeq ($(CONFIG_BOARD_SAMA5D2_XPLAINED),y)
CFLAGS += -DCONFIG_BOARD_SAMA5D2_XPLAINED
IAR_FLAGS += CONFIG_BOARD_SAMA5D2_XPLAINED
endif
ifeq ($(CONFIG_BOARD_SAMA5D4_EK),y)
CFLAGS += -DCONFIG_BOARD_SAMA5D4_EK
IAR_FLAGS += CONFIG_BOARD_SAMA5D4_EK
endif
ifeq ($(CONFIG_BOARD_SAMA5D4_XPLAINED),y)
CFLAGS += -DCONFIG_BOARD_SAMA5D4_XPLAINED
IAR_FLAGS += CONFIG_BOARD_SAMA5D4_XPLAINED
endif

ifeq ($(CONFIG_HAVE_DBGU),y)
CFLAGS += -DCONFIG_HAVE_DBGU
IAR_FLAGS += CONFIG_HAVE_DBGU
endif
ifeq ($(CONFIG_HAVE_FLEXCOM),y)
CFLAGS += -DCONFIG_HAVE_FLEXCOM
IAR_FLAGS += CONFIG_HAVE_FLEXCOM
endif
ifeq ($(CONFIG_HAVE_PIO3),y)
CFLAGS += -DCONFIG_HAVE_PIO3
IAR_FLAGS += CONFIG_HAVE_PIO3
endif
ifeq ($(CONFIG_HAVE_PIO4),y)
CFLAGS += -DCONFIG_HAVE_PIO4
IAR_FLAGS += CONFIG_HAVE_PIO4
endif
ifeq ($(CONFIG_HAVE_AT25DFX),y)
CFLAGS += -DCONFIG_HAVE_AT25DFX
IAR_FLAGS += CONFIG_HAVE_AT25DFX
endif
ifeq ($(CONFIG_HAVE_AT24MAC402),y)
CFLAGS += -DCONFIG_HAVE_AT24MAC402
IAR_FLAGS += CONFIG_HAVE_AT24MAC402
endif
ifeq ($(CONFIG_HAVE_PMIC_ACT8945A),y)
CFLAGS += -DCONFIG_HAVE_PMIC_ACT8945A
IAR_FLAGS += CONFIG_HAVE_PMIC_ACT8945A
endif
ifeq ($(CONFIG_HAVE_USART_FIFO),y)
CFLAGS += -DCONFIG_HAVE_USART_FIFO
IAR_FLAGS += CONFIG_HAVE_USART_FIFO
endif
ifeq ($(CONFIG_HAVE_SPI_FIFO),y)
CFLAGS += -DCONFIG_HAVE_SPI_FIFO
IAR_FLAGS += CONFIG_HAVE_SPI_FIFO
endif
ifeq ($(CONFIG_HAVE_DDR3),y)
CFLAGS += -DCONFIG_HAVE_DDR3
IAR_FLAGS += CONFIG_HAVE_DDR3
endif
